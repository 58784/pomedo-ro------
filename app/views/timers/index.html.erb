<% content_for(:title, t('.title')) %>

<h3>タイマー一覧</h3>
<div  class="text-center m-3">
    <%= link_to "+ タイマーを作成する", new_timer_path,class: "px-5 btn btn-outline-success" %>
</div>
<% if current_user.timers.exists? %>
    <table class="table table-striped table-hover thead">
        <thead>
            <tr>
                <th><%= Timer.human_attribute_name("name")%></th>
                <th><%= Timer.human_attribute_name("work_time")%></th>
                <th><%= Timer.human_attribute_name("rest_time")%></th>
                <th><%= Timer.human_attribute_name("start_time")%></th>
                <th><%= Timer.human_attribute_name("finish_time")%></th>
                <th><%= Timer.human_attribute_name("character")%></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <% current_user.timers.each do |f| %>
                    <div>
                        <td><%= f.name%></td>
                        <td><%= f.work_time.strftime('%H:%M')%></td>
                        <td><%= f.rest_time.strftime('%H:%M')%></td>
                        <td><%= f.start_time.strftime('%H:%M')%></td>
                        <td><%= f.finish_time.strftime('%H:%M')%></td>
                        <td><%= f.character_i18n%></td>
                        <td><%= link_to "編集", edit_timer_path(f), class: "btn btn-outline-info"%></td>
                        <td><%= link_to "削除", timer_path(f), method: :delete, class: "btn btn-outline-danger"%></td>
                        <td></td>
                    </div>
            </tr>
                <%end%>
        </tbody>
    </table>
<%else%>
    <p>タイマーはありません。新しく作成しましょう！</p>
<%end%>

<div  class="text-center m-3">
    <%= link_to "+ タイマーを作成する", new_timer_path,class: "px-5 btn btn-outline-success" %>
</div>
<%= link_to "マイページ", edit_user_path(current_user) %>